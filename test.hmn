출력("너는 무엇이지?") 를 실행하고

사용자입력 은 입력받기(": ") 이고

만약 정수(사용자입력) 이 1 이면
    출력("넌 1이군") 을 실행함
을 함

아니고 정수(사용자입력) 이 2 이면
    출력("넌 2이군") 을 실행하고

    출력("정말 좋군") 을 실행함
을 함
아니고 정수(사용자입력) 이 3 이면
    출력("3이라니!!!") 를 실행하고
    출력("예상치 못해따!!!") 를 실행함

을 함

아니면
    출력("아니라니!!!!!!!!!!!!!") 를 실행함
을 함

출력("이게 종료인것 같다") 를 실행함

출력(출력("나다"), 입력받기(출력("ㅊ"))) 을 실행함
너 는 5 임


리스트 는 [[1, 2, 3], {너 : 너, 너 : 너}, 더하기 6, 너 나누기 3] 임

리스트 에 append(90) 을 실행함

뭔가길이 는 리스트 의 append 임

출력(리스트, 뭔가길이) 를 실행함



